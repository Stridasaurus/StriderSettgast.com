---
import BaseLayout from '../layouts/BaseLayout.astro';
import SectionCard from '../components/SectionCard.astro';
---

<BaseLayout title="Strider Settgast">
  <div class="container">

    <section class="hero">
      <div class="headshot" aria-label="Headshot placeholder">
        <svg viewBox="0 0 80 80" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <circle cx="40" cy="40" r="40" fill="#e0e0e0" />
          <circle cx="40" cy="32" r="13" fill="#c0c0c0" />
          <ellipse cx="40" cy="70" rx="22" ry="16" fill="#c0c0c0" />
        </svg>
      </div>

      <div class="hero-text">
        <h1>Strider Settgast</h1>
        <p class="tagline">Machine learning student &amp; developer</p>
        <p class="bio">
          I'm based in Florida, focused on deep learning and computer vision.
          I build models, write about what I learn, and share work in progress.
        </p>
      </div>
    </section>

    <section class="sections" aria-label="Site sections">
      <SectionCard
        title="About"
        description="Background, interests, and what I'm currently working on."
        href="/about"
      />
      <SectionCard
        title="Projects"
        description="Deep learning and computer vision projects, with code and writeups."
        href="/projects"
      />
      <SectionCard
        title="Blog"
        description="Notes on machine learning, research papers, and things I'm figuring out."
        href="/blog"
      />
    </section>

  </div>
</BaseLayout>

<style>
  .container {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 2rem;
  }

  .hero {
    display: flex;
    align-items: flex-start;
    gap: 2rem;
    padding: 4rem 0 3.5rem;
  }

  .headshot {
    width: 80px;
    height: 80px;
    flex-shrink: 0;
  }

  .headshot svg {
    width: 100%;
    height: 100%;
    border-radius: 50%;
  }

  .hero-text h1 {
    font-size: 1.5rem;
    font-weight: 500;
    color: var(--text-primary);
    letter-spacing: -0.02em;
    line-height: 1.2;
    position: relative;
    display: inline-block;
  }

  .hero-text h1::after {
    content: '';
    display: block;
    height: 2px;
    width: 100%;
    background: var(--accent);
    margin-top: 5px;
  }

  .tagline {
    font-size: 0.9375rem;
    color: var(--text-secondary);
    margin-top: 0.625rem;
    margin-bottom: 1rem;
  }

  .bio {
    font-size: 0.9375rem;
    color: #444444;
    line-height: 1.7;
    max-width: 500px;
  }

  .sections {
    border-top: 1px solid var(--border);
    padding-bottom: 4rem;
  }

  @media (max-width: 480px) {
    .hero {
      flex-direction: column;
      gap: 1.5rem;
      padding: 2.5rem 0 2.5rem;
    }
  }
</style>
