---
title: "Your Post Title"
date: 2026-06-15
description: "One sentence shown on the blog index and in search/social previews."
tags: ["physics", "computation"]
---

Open with a short paragraph framing the question or idea. The first lines set
the tone — write like you're explaining it to a sharp friend.

## A section heading

Regular prose, **bold**, *italics*, and [links](https://example.com) all work.

Inline math uses single dollar signs: the field $\mathbf{B}(\mathbf{r}, t)$ varies
in space and time. Display math uses double dollar signs:

$$
\nabla \times \mathbf{B} = \mu_0 \mathbf{J} + \mu_0 \varepsilon_0 \frac{\partial \mathbf{E}}{\partial t}
$$

Code blocks get syntax highlighting:

```python
import numpy as np

def fft_magnitude(signal: np.ndarray) -> np.ndarray:
    return np.abs(np.fft.rfft(signal))
```

- Bullet lists
- work fine

> Blockquotes work too.

Close with a takeaway or a pointer to what's next.
